#   create_file_from_string(my_filename, my_string)
#        som skapar en fil med namnet my_filename (en sträng), och fyller den med innehållet i  my_string (en sträng)
#        Exempel på användning: create_file_from_string("min_superfil.txt", "hurra för mig")